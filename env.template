# Environment Variables Template
# Copy this file to .env and fill in your actual values

# ============================================
# OpenAI API Configuration
# ============================================
OPENAI_API_KEY=your_openai_api_key_here
# Optional: Custom API Base URL (e.g. for local LLMs or proxies)
# API_BASE_URL=http://localhost:11434/v1

# ============================================
# Langfuse Configuration
# ============================================
# Get API keys from Langfuse UI → Settings → API Keys

LANGFUSE_PUBLIC_KEY=your_langfuse_public_key_here  # Starts with pk-lf-
LANGFUSE_SECRET_KEY=your_langfuse_secret_key_here  # Starts with sk-lf-

# Langfuse Host URL
# For Self-Hosted (Docker):
LANGFUSE_HOST=http://localhost:3000

# For Langfuse Cloud (uncomment if using cloud):
# LANGFUSE_HOST=https://cloud.langfuse.com  # EU region
# LANGFUSE_HOST=https://us.cloud.langfuse.com  # US region

# ============================================
# Agent Configuration
# ============================================
# Agent display name
AGENT_NAME=Climate Agriculture Analyst

# Model to use - Options:
#   - gpt-4o (alias, always uses latest stable version)
#   - gpt-4o-2024-08-06 (specific version - recommended for consistency)
#   - gpt-4o-mini (cheaper, faster alternative)
AGENT_MODEL=gpt-4o-2024-08-06
API_BASE_URL=

# Prompt name in Langfuse
PROMPT_NAME=climate-agent-instructions

# ============================================
# Logging Configuration (Optional)
# ============================================
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR

# ============================================
# Database Configuration (Optional)
# ============================================
# TABLE_NAME=climate_agriculture_data

